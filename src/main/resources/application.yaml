spring:
  servlet:
    multipart:
      max-file-size: 3MB
      max-request-size: 12MB
  mvc:
    static-path-pattern: /static/**
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/testboard
    username: root
    password: green502

mybatis:
  type-aliases-package: com.green.boardtest
  mapper-locations: mapper/**/*.xml

# swgger
springdoc:
  swagger-ui:
    path: /swagger.html

file:
  dir: D:/download/boardtest
